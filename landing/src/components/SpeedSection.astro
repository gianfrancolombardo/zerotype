---
import { Keyboard, Mic } from "lucide-astro";
---

<section class="py-32 relative overflow-hidden">
    <!-- Background Glow -->
    <div
        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[300px] bg-indigo-500/5 rounded-full blur-[100px] pointer-events-none"
    >
    </div>

    <div class="max-w-5xl mx-auto px-6 relative z-10">
        <div class="text-center mb-20">
            <h2 class="text-4xl md:text-5xl font-bold mb-6 tracking-tight">
                El fin de la era del teclado
            </h2>
            <p class="text-xl text-zinc-400 max-w-2xl mx-auto">
                Llevamos 150 años atados a las teclas. Tu voz es la herramienta
                más rápida que existe. Úsala.
            </p>
        </div>

        <!-- Comparison Card -->
        <div
            class="relative grid grid-cols-1 md:grid-cols-2 gap-8 lg:gap-12 items-center"
        >
            <!-- Keyboard Side (The Past) -->
            <div
                class="group relative p-8 rounded-3xl bg-zinc-900/30 border border-white/5 backdrop-blur-sm grayscale opacity-70 hover:opacity-100 hover:grayscale-0 transition-all duration-500"
            >
                <div class="flex items-center justify-between mb-8">
                    <div class="flex items-center gap-3">
                        <div class="p-2 rounded-lg bg-zinc-800">
                            <Keyboard class="w-6 h-6 text-zinc-400" />
                        </div>
                        <span class="font-medium text-zinc-300">Teclado</span>
                    </div>
                    <span class="text-3xl font-bold text-zinc-500 font-display"
                        >40 <span class="text-base font-normal text-zinc-600"
                            >ppm</span
                        ></span
                    >
                </div>

                <div class="space-y-3">
                    <!-- Fake typing lines -->
                    <div
                        class="h-2 w-3/4 bg-zinc-800 rounded-full overflow-hidden"
                    >
                        <div
                            class="h-full bg-zinc-600 w-full animate-[loading_4s_steps(20)_infinite]"
                        >
                        </div>
                    </div>
                    <div
                        class="h-2 w-1/2 bg-zinc-800 rounded-full overflow-hidden"
                    >
                        <div
                            class="h-full bg-zinc-600 w-full animate-[loading_3s_steps(15)_infinite]"
                        >
                        </div>
                    </div>
                    <div
                        class="h-2 w-2/3 bg-zinc-800 rounded-full overflow-hidden"
                    >
                        <div
                            class="h-full bg-zinc-600 w-full animate-[loading_5s_steps(25)_infinite]"
                        >
                        </div>
                    </div>
                </div>

                <div class="mt-8 text-sm text-zinc-600">
                    Lento. Propenso a errores. Causa fatiga.
                </div>
            </div>

            <!-- Arrow Indicator (only visible on desktop) -->
            <div
                class="hidden md:flex absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-12 h-12 bg-zinc-900 rounded-full border border-zinc-800 items-center justify-center z-20"
            >
                <span class="text-zinc-500 font-bold text-xs">VS</span>
            </div>

            <!-- Voice Side (The Future) -->
            <div
                class="relative p-8 rounded-3xl bg-gradient-to-br from-indigo-900/20 to-zinc-900/40 border border-indigo-500/20 backdrop-blur-md shadow-[0_0_50px_rgba(99,102,241,0.1)]"
            >
                <div
                    class="absolute inset-0 bg-indigo-500/5 blur-xl rounded-3xl -z-10 animate-pulse"
                >
                </div>

                <div class="flex items-center justify-between mb-8">
                    <div class="flex items-center gap-3">
                        <div class="p-2 rounded-lg bg-indigo-500/20">
                            <Mic class="w-6 h-6 text-indigo-400" />
                        </div>
                        <span class="font-medium text-white">Zerotype</span>
                    </div>
                    <span
                        class="text-5xl font-bold text-white font-display text-glow"
                        >200+ <span
                            class="text-base font-normal text-indigo-300"
                            >ppm</span
                        ></span
                    >
                </div>

                <div class="space-y-4">
                    <!-- Voice lines -->
                    <div class="flex items-center gap-2">
                        <div
                            class="h-1.5 flex-1 bg-indigo-500/20 rounded-full overflow-hidden"
                        >
                            <div
                                class="h-full bg-indigo-500 w-full animate-[slide-right_1s_ease-out_infinite]"
                            >
                            </div>
                        </div>
                    </div>
                    <div class="flex items-center gap-2">
                        <div
                            class="h-1.5 flex-1 bg-indigo-500/20 rounded-full overflow-hidden"
                        >
                            <div
                                class="h-full bg-indigo-400 w-full animate-[slide-right_1.2s_ease-out_infinite_0.2s]"
                            >
                            </div>
                        </div>
                    </div>
                    <div class="flex items-center gap-2">
                        <div
                            class="h-1.5 flex-1 bg-indigo-500/20 rounded-full overflow-hidden"
                        >
                            <div
                                class="h-full bg-indigo-300 w-full animate-[slide-right_0.8s_ease-out_infinite_0.1s]"
                            >
                            </div>
                        </div>
                    </div>
                </div>

                <div
                    class="mt-8 text-sm text-indigo-200/60 font-medium flex items-center gap-2"
                >
                    <span
                        class="w-2 h-2 rounded-full bg-indigo-400 animate-pulse"
                    ></span>
                    Velocidad de pensamiento. Cero fricción.
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    @keyframes loading {
        0% {
            width: 0;
        }
        50% {
            width: 100%;
        }
        100% {
            width: 100%;
        }
    }

    @keyframes slide-right {
        0% {
            transform: translateX(-100%);
        }
        100% {
            transform: translateX(100%);
        }
    }
</style>
